#!/bin/sh
# Os dados do usuário (banco SQLite e certificados) ficam em ./dados do diretório
# de execução e não são removidos automaticamente pelo gerenciador de pacotes.
echo "Os dados locais do Validador eSocial (diretório 'dados/') não foram removidos."
exit 0
